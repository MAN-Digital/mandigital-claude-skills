# Deep Dives

Use these notes before cloning high-value templates. They summarize observed structure from `batch_get`; still inspect the live node before edits.

## `S20AXj` - Prospecting Agent vs Outbound Cover

Best for a dark vertical cover introducing a comparison between HubSpot Prospecting Agent and dedicated outbound platforms. Read `components/S20AXj-anatomy.md` before reusing pieces from this frame.

Structure:

- Dark Medium Blue 1190 x 1684 cover.
- Title system: `CcSWR` Prospecting Agent, `oEmxe` vs, `Y3EFnY` Outbound Platforms.
- Angled background vectors: `oCVLE` and `EzX4X`.
- Dot-grid decorations: `U625h3` and `c1w4M`.
- Topic line: `u4YLL` Data / Governance / Deliverability.
- Subtitle copy: `jrrkW`.
- Large light visual placeholder: `zLNDk`.
- Bottom-right callout: `w8dYDV` and `KOasO`.

Edit risks:

- The title uses very large text. Keep replacement copy short.
- The light placeholder should contain a real approved diagram/screenshot or remain intentionally blank; do not fill it with generic decoration.
- Keep `w8dYDV` and `KOasO` paired.

## `nRPmP` - Signal-To-Revenue Dark Poster

Best for a strong vertical thesis graphic. Read `components/nRPmP-anatomy.md` before reusing pieces from this frame. Structure:

- Dark blue 1190 x 1684 base.
- Top logo group and centered title/subtitle block.
- Large central circular signal system `kC04H` with orbit nodes, central HubSpot hub `i5rrd`, gradients, white/orange connector accents, and Streamline icons.
- Left Signal Inputs card `wgwpV` with stacked pill components.
- Bottom sequential process strip `VSVo4` with five connected "Right ..." step cards.
- Bottom closing line `vlJgV`.
- Main editable text nodes include the title, subtitle, orbit labels, signal pill labels, process-strip cards, and closing statement.

Edit risks:

- The orbit is absolute-positioned and fragile. Move/copy the whole group rather than rebuilding pieces.
- `Wdvjp` is an orbit-circle marker, while the nearby icon/label step is a separate frame such as `t8o5y`.
- The process strip connectors depend on the five-card spacing. If card count changes, re-layout and screenshot.
- Orange appears in connector accents; keep it restrained.
- Pill labels should stay short.

## `KVqAt` - Five-Stage Signal Engine

Best for explaining architecture: inputs, data layer, AI interpretation, action layer, revenue outcome. Read `components/KVqAt-anatomy.md` before reusing pieces from this frame.

Structure:

- Light blue 1190 x 1684 background with a large pale ellipse.
- Five card system: three upper cards and two lower cards with connector paths.
- Stage cards: `voulE` Signal Inputs, `GGxl2` Signal HubSpot Data Layer, `rRoSd` AI Interpretation, `LQxPS` Action Layer, `l6jnb` Revenue Outcome.
- Icon medallions above cards: `g0uwc`, `cRGY1`, `CK187`, `L6sz6`, `CW9op`.
- Pill rows inside each stage card are indexed in `components/KVqAt-pill-rows.md`.
- Dots and subtle connector arrows provide system feel.

Edit risks:

- The frame uses many absolute connectors; if card count changes, redraw connectors carefully.
- Keep the five-stage model when possible. For fewer steps, choose a different template.
- Keep AI Interpretation between the HubSpot Data Layer and Action Layer; that is the key point of the diagram.

## `tMsEe` - Signal Sources To Revenue Engine

Best for showing native, integrated, and custom signal sources feeding a HubSpot revenue engine and producing outcomes. Read `components/tMsEe-anatomy.md` before reusing pieces from this frame.

Structure:

- Three major columns: Signal Sources, HubSpot Revenue Engine, Outcomes.
- Source cards: `ftaD9` Native HubSpot, `cyraK` Integrations, `NDHPc` / `cDfXv` Custom.
- Center engine card: `TFZvq` with timeline content `gyGY4`.
- Six engine steps: `uOjvY` Detect, `penfJ` Enrich, `eLsK1` Score, `t16Aki` Segment, `X5rVj` Engage, `VXBXH` Measure.
- Outcome rows: `jmis9`, `Ikpzh`, `XMlIk`, `E93plV`, `UsYRl`, `E4Ylf`.
- Orange vertical timeline/dot system and blue column connectors.
- Title and right-side explanatory paragraph.

Edit risks:

- This template can tolerate label changes, but not heavy long-form copy.
- Keep row counts stable unless you are ready to re-space the timeline.
- Integration logo/image placeholders under `cyraK` are expected in the current source. Replace only with approved assets.

## `b8SoH` - Signal To Sales Play

Best for translating signal detection into sales plays and downstream actions. Read `components/b8SoH-anatomy.md` before reusing pieces from this frame.

Structure:

- Center Signal Detected hub: `OzxqD` on hub surfaces `t9i2m6` / `RJSUv`.
- Four play cards: `v0oSD6` Visitor Intent Play, `tnsHv` Research Intent Play, `YQjgo` Company Event Play, `t4PYrn` Custom Signal Play.
- Vertical action chain: `o8JsKS` Score + Route, `tPufj` AI Message Draft, `u9s7MI` Rep Reviews, `q5n3w` Engage, `gHgAL` Measure Result.
- Connector brackets collect the four play cards into the vertical action chain.
- Pale radar rings and dot-grid decorations create signal context.

Edit risks:

- The central circle should stay dominant.
- Keep action labels verb-led and short.
- The connector geometry assumes four play cards and five action rows. Re-layout and screenshot if counts change.

## `llyux` - Operating Model Layers

Best for full operating models, implementation frameworks, and layered checklists. Read `components/llyux-anatomy.md` before reusing pieces from this frame.

Structure:

- Light blue vertical frame.
- Four layered rows: Inputs, Processing, Activation, Measure + Learn.
- Each row has a numbered side rail, vertical label, short description, white content panel, and compact capability tiles.
- Inputs tiles: `cxs00`, `LmSrf`, `g77fJ3`, `Cr3TW`, `gx2Vb`.
- Processing tiles: `QohGy`, `W1wPN`, `sNcb7`, `hdgMM`, `kWGxf`.
- Activation tiles: `axrAu`, `v5vVj`, `d45hgj`, `kDHQJ`, `t7CJQ`.
- Measure + Learn tiles: `p9Pio`, `kPyZN`, `nClmy`, `N6hQ6i`.
- Bottom outcome chain `xAG2o`: Better signals, Smarter actions, More pipeline, Sustainable growth.

Edit risks:

- Many text blocks are small; avoid long copy.
- Preserve row order and layer hierarchy.
- The connector system assumes four layer rows and the bottom outcome chain. Re-layout and screenshot if counts change.

## Prospecting Agent vs Outbound Sequence

Best for a full vertical carousel/explainer sequence about HubSpot Prospecting Agent, outbound tools, CRM governance, and hybrid prospecting architecture.

Frames:

- `S20AXj`: dark cover.
- `l5NNCU`: Venn-style comparison of HubSpot strengths, outbound strengths, and a best-practice hybrid.
- `EdtGZ`: hybrid prospecting architecture with signal inputs, HubSpot lane, research/qualification layer, outbound lane, decision diamond, deliverability, and handoff. Read `components/EdtGZ-anatomy.md` before reusing individual parts.
- `LZkoW`: outbound-to-HubSpot sync model with outside HubSpot, sync rules, inside HubSpot, and governance benefits.
- `sQ8TR`: air-cover loop showing brand/research reinforcement around outbound.
- `pAw6X`: recommended five-layer prospecting operating model.
- `fKn3K`: closing key takeaways.

Edit risks:

- The sequence repeats footer/navigation grammar; keep slide numbers and MAN Digital footer consistent.
- The source `fKn3K` has a repeated heading for items 2 and 3. Rewrite item 3 when producing final content unless the repetition is intentional.
- Use `LZkoW` for governance and sync rules, `pAw6X` for prescribed operating model layers, and `sQ8TR` for demand/air-cover loops.

Important `EdtGZ` parts:

- Header/title: `N9vBe`, `q1ZWI`, `EWQkm`.
- Lanes: `qBrvo` HubSpot lane, `L4Tph8` Outbound lane.
- Decision routing: `tMycI`, `qctTA`, `L4BsQ`, connectors `nKaVf`, `T69e7c`, `j7TFin`, `P6yn6G`.
- Right callouts: `LcGj0`, `NMnG1`.
- Bottom sequence: `wTMqf`.
- Footer: `wyrZ3`.

## `V55uo` - Classic Centered 16:9

Best for blog hero and Open Graph cover graphics.

Structure:

- 1920 x 1080 blue background.
- Logo at top-left.
- Centered vertical content stack: large title, subtitle, icon.
- Accent circle/glow.

Edit risks:

- Title should be short enough to avoid a wall of text.
- Do not add complex diagrams inside this cover.

## `5MGHD` - Problem/Solution Square

Best for a square problem/framing slide.

Structure:

- Ghost White background.
- Indicator chips at top-left.
- Icon square, title, subtitle.
- Three stacked blue content cards.
- Footer logo and slide number.

Edit risks:

- Three-card stack expects compact statements.
- Keep the slide number/footer if using as carousel; remove only for standalone blog crop.

## `dSYM9` - Decision Ladder

Best for scoring, qualification, and evidence-based decisions.

Structure:

- Header with icon, title, and subtitle.
- White decision ladder card with multiple rungs and a strong final decision badge.
- Footer logo and slide number.

Edit risks:

- Ladder rungs must remain parallel.
- Keep final badge as the decisive action or conclusion.

## `eYM78` - Impact Grid

Best for four-lever impact stories.

Structure:

- Header with icon/title/subtitle.
- Large 2x2 grid area.
- Subtle circular decoration.
- Footer.

Edit risks:

- Works best with exactly four levers.
- Avoid turning each cell into paragraph text.

## `jWyvs` - Bow-Tie Lifecycle

Best for lifecycle and funnel-to-expansion stories.

Structure:

- Header with timeline icon, title, subtitle.
- Central bow-tie group with acquisition and expansion labels.
- Multiple stage nodes along the bow-tie path.
- Footer.

Edit risks:

- Keep the bow-tie labels conceptually paired.
- Do not add more stages unless the path remains readable.

## `gASqb` - Score Card

Best for readiness assessments and diagnostic scorecards.

Structure:

- Header with trophy icon, title, subtitle.
- White scorecard with three green rungs and final score/decision badge.
- Footer.

Edit risks:

- Green is present in the source template but is not a core MAN Digital brand color. Use it only when the scorecard semantics justify it, or convert to blue/cyan if the graphic should be stricter brand.

## `PPpdM` - Prospecting-Agent Cover

Best for 1080 x 1350 LinkedIn/blog-social lead graphics about prospecting agents.

Structure:

- Dark blue outer frame.
- Large light panel.
- Centered title/subtitle.
- Circular prospecting-agent system diagram.
- Four surrounding label groups: signals, reporting, research, outreach.

Edit risks:

- The source has typo-prone labels such as "Reasearch"; fix copy when reusing.
- The circular system is useful, but dense. Screenshot after editing.

## Prospecting-Agent 1080x1350 Sequence

Best for LinkedIn carousel or 4:5 blog-social graphics that explain HubSpot Prospecting Agent as a connected operating system, not a standalone AI feature.

Sequence map:

- `PPpdM`: cover/lead frame with central Prospecting Agent system and four surrounding components: Signals, Reporting, Research, Outreach.
- `s2ZqRB`: underperformance diagnosis with four failure circles: No ICP, Weak signals, Incomplete data, No workflow.
- `v5aVuG`: point tools vs Prospecting Agent comparison; linear task stack on the left, connected hub on the right.
- `lLDFt`: disconnected pre-sales tools; Signal, Data, Research, Outreach, and CRM sync-back with context-loss annotations.
- `SbBJV`: what the agent needs; Signals, CRM context, Agent setup, Human review around the central agent.
- `G7EWZ`: target market setup with two screenshot placeholders and ICP/account-filter tags.
- `G6YkCq`: Buyer Intent activation with one large screenshot placeholder and signal/research-intent tags.
- `nia9w`: active list / working list bridge with two screenshot placeholders and signal-to-action tags.
- `DJN2N`: move accounts into Prospecting Agent with one large screenshot placeholder and action labels.
- `jNmrp`: review drafts and track results with one large screenshot placeholder and approval/reporting tags.
- `NhErk`: four layer summary: Signal Intelligence, Data Foundation, Agent Configuration, Human Workflows.
- `Y3Tzyc`: 90-day rollout path: Discover, Plan, Pilot, Optimize, Scale.
- `zuu8y`: CRM Ops before enrollment: one-to-one email subscription and email verification.
- `lP11x`: HubSpot credits/economics: credit value, conversations, intent signals.
- `HoOjV`: dark closing CTA/save-this-carousel frame.

Edit risks:

- Several frames are screenshot carriers. Replace checkerboard placeholders with approved screenshots before export.
- Source text contains typo-prone strings: `Reasearch`, `Prostecting`, `Rady`, and `Al demo`. Correct them when creating final graphics.
- `lP11x` contains HubSpot credit/pricing figures. Verify current pricing before publishing.
- Keep the vertical logo/footer rhythm consistent across the sequence.
