# Copy to .env locally (do not commit .env) or export these directly in your shell.
# Required only for scripts/analyze_source_video.py (video-grounded storyboard mode).
# Get a key at https://aistudio.google.com/apikey
GEMINI_API_KEY=
