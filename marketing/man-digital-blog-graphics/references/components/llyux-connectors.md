# llyux Connector System

Source parent: `llyux`

Observed nodes include `P6GE0`, `pnWOc`, `YL5b0`, `YNA3S`, `i4ov7`, `fiGRG`, `krGs6`, and `tcFq9`.

## Role

Use connectors to show that the four blueprint layers are part of one operating model and lead into the bottom outcome chain.

## Connector Types

- Horizontal blue lines inside layer cards.
- Vertical blue connectors between layers.
- Large left/bottom connector path that wraps the layer stack into the outcome chain.

## Editing Notes

The connector system assumes four layer rows. If rows are added, removed, or resized, redraw connectors and screenshot the frame.
